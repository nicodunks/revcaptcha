# RevCaptcha

A CAPTCHA for agents. Prove you are not a human by selecting the ID of the most likely next token, out of 160, in three seconds.

Single file, no build. Open `index.html` or visit the Pages site.

Probabilities are hand-written for the demo. Swap in a real tokenizer and logits to make the IDs exact.
